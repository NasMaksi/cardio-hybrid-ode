#Гибридная нейронная модель ОДУ с ReservoirNet для сердечно-сосудистой системы
Это обновлённая реализация гибридной нейронной модели сердечно-сосудистой системы на Python с использованием PyTorch и библиотеки torchdiffeq.  
Новая версия расширяет предыдущий код за счёт применения нейросети типа ReservoirNet и более гибкого интерфейса запуска.

##Описание
- Модель сочетает **физиологическую ODE-модель** и **нейросеть ReservoirNet**, которая предсказывает:
  - дополнительное периферическое давление `Pperi`
  - объём смещения межжелудочковой перегородки `Vspt`
- Используется численное решение дифференциальных уравнений (нейронные ОДУ).
- Пользователь может задавать:
  - начальные условия (10 переменных состояния)
  - длительность интегрирования во времени
- Переменные состояния отображаются как `var1 … var10`:
  - `var1` = Qmt (митральный поток)  
  - `var2` = Qav (аортальный поток)  
  - `var3` = Qtc (трикуспидальный поток)  
  - `var4` = Qpv (лёгочный поток)  
  - `var5` = Vlv (объём левого желудочка)  
  - `var6` = Vao (объём аорты)  
  - `var7` = Vvc (венозный объём)  
  - `var8` = Vrv (объём правого желудочка)  
  - `var9` = Vpa (объём лёгочной артерии)  
  - `var10` = Vpu (объём лёгочных вен)

##Установка зависимостей
Убедитесь, что у вас установлен Python 3.8+ и выполните:

```bash
pip install torch torchdiffeq numpy matplotlib
